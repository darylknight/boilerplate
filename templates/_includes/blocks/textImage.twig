{% import '_includes/macros' as macros %}

{% set asset = block.image.one %}

{% set transform = {
	width: 1050
} %}

<div class="container mx-auto p-4 lg:py-10">
	<div class="grid grid-cols-12">
		<div class="col-span-12 lg:col-span-7 {{ block.layout == 'textImage' ? 'lg:col-start-6 lg:order-last' }}">
			{{ macros.image(asset, transform, 'mb-4 lg:mb-0') }}
		</div>
		<div class="col-span-12 lg:col-span-4 {{ block.layout == 'textImage' ? '' : 'lg:col-start-9' }}">
			{{ macros.text(block.text) }}

			{% if block.button|length %}
				<div class="mb-10"></div>
				{{ macros.button(block.button.url, block.button.text, 'btn', block.button.target) }}
			{% endif %}
		</div>
	</div>
</div>
