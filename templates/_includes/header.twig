<header class="bg-gray">
	<div class="container mx-auto px-4 py-1">
		<nav>
			{% for entry in craft.entries.section('menuHeader').all() %}
				<a href="{{ entry.pageTarget.url }}" target="{{ entry.pageTarget.target }}">{{ entry.pageTarget.text }}</a>
			{% endfor %}
		</nav>
	</div>
</header>
