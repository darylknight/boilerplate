{% import '_includes/macros' as macros %}

{% extends '_layout' %}

{% block content %}
	<div class="prose container mx-auto p-5 text-center lg:py-32">
		<h1 class="h1">{{ error404.heading }}</h1>

		{{ error404.text }}
	</div>
{% endblock %}
